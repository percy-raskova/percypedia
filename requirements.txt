# CI build dependencies (minimal for Cloudflare Pages)
# For full development deps: pip install -r requirements-dev.txt

# Sphinx core
sphinx>=6.0
myst-parser>=1.0

# Theme
furo

# Extensions
sphinx-design
sphinx_tags
sphinx-hoverxref

# Schema validation (used by extensions)
jsonschema>=4.0
pyyaml
sphinx-sitemap
